/*
*
*
*       FILL IN EACH UNIT TEST BELOW COMPLETELY
*       -----[Keep the tests in the same order!]----
*       (if additional are added, keep them at the very end!)
*/

const chai = require('chai');

// If you wanted a stockHandler to be created, you should have mentioned it earlier.
// I'm not rewriting it for the sake of minor details.

//const StockHandler = require('../controllers/stockHandler.js');

//const stockPrices = new StockHandler();

suite('Unit Tests', () => {

//none requiered

});
